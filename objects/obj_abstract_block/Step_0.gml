event_inherited();

/*if mouse_check_button(mb_left)
{
	var _dist = point_distance(x, y, mouse_x, mouse_y)
	if point_distance(x, y, mouse_x, mouse_y) < 100
	{
		var _str = 1. * (120 - _dist);
		var _dx = mouse_x - x;
		var _dy = mouse_y - y;
		physics_apply_force(x, y, _str * _dx / sqrt(_dx*_dx + _dy*_dy), _str * _dy / sqrt(_dx*_dx + _dy*_dy));
	}
}*/